# Just pass off into the layer itself.
# At some point this will have some tasks to copy the layer to publishing
# Or maybe even add into android
cmake_minimum_required(VERSION 3.0.0 FATAL_ERROR)
add_subdirectory(VK_Layer_LMG_raytracing_sim)